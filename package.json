{
  "name": "retailflow-pos",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@prisma/client": "^5.20.0",
    "husky": "^9.1.0",
    "pnpm": "^9.12.0",
    "prisma": "^5.20.0",
    "turbo": "^2.1.0"
  }
}
